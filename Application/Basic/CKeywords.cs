using System;
namespace Application.Basic
{
    class CKeywords
    {
        /*
         * Keywords are reserved words predefined to the C# compiler
         * These keywords cannot be used as identifiers
         * if these keywords are to be used as identifiers, these needs to be prefixed the keyword with the @ character
         * 
         * some identifiers have special meaning in context of code, such as get and set are called contextual keywords.
         * 
         */

        static void Example()
        {
            {
                /*
                 * Data Types Keywords
                 * -------------------
                 * bool
                 * char
                 * decimal
                 * double
                 * int
                 * float
                 * long
                 * short
                 * byte
                 * sbyte
                 * void
                 * string
                 * ulong
                 * uint
                 * ushort
                 * enum
                 * class
                 * struct
                 */

                /*
                 * Structural Keywords
                 * -------------------
                 * if
                 * else
                 * do
                 * while
                 * foreach
                 * for
                 * continue
                 * break
                 * goto
                 * switch
                 * case
                 * default
                 * try
                 * catch
                 * throw
                 * finally
                 * namespace
                 * private
                 * public
                 * protected
                 * static
                 * this
                 * return
                 * virtual
                 * using
                 */

                /*
                 * abstract
                 * base
                 * checked
                 * const
                 * delegate
                 * event
                 * explicit
                 * extern
                 * false
                 * fixed
                 * as 
                 * implicit
                 * in
                 * interface
                 * internal
                 * is
                 * lock
                 * new
                 * null
                 * object
                 * operator
                 * out
                 * override
                 * params
                 * readonly
                 * ref
                 * sealed
                 * sizeof
                 * stackalloc
                 * true
                 * typeof
                 * unchecked
                 * unsafe
                 * 
                 * 
                 * volatile
                 */

                /* 
                 * Contextual Keywords
                 * add
                 * alias
                 * ascending
                 * descending
                 * dynamic
                 * from
                 * get
                 * global
                 * group
                 * into
                 * join
                 * let
                 * orderby
                 * partial (type)
                 * partial(method)
                 * remove
                 * select
                 * set
                 */
            }
        }
    }
}
